1.下列那些组件属于实例？
  A.SGA
  B.server process
  C.pga
  D.background process

2.下列那些操作不会触发数据写？
  A.截断表
  B.commit;
  C.alter system checkpoint;
  D.alter tablespace XXX beging backup;

3.下列那些文件是数据库运行时所必需的？
  A.pfile
  B.password file
  C.archived log file
  D.control file

4.控制文件的初始大小由什么决定？
  A.现有数据文件的个数
  B.现有日志文件的个数
  C.建库时所规定的日志文件和数据文件的个数
  D.建库时所规定的数据文件的个数

5.实例的初始行为由谁约束?
  A.参数文件
  B.数据库的运行状况
  C.控制文件
  D.数据库版本

6.oracle启动的正确顺序?
  A.mount nomount open
  B.nomount mount open
  C.mount open nomount
  D.nomount open mount

7.下列哪些操作在数据库重起时会发生实例恢复?
  A.shutdown immediate
  B.shutdown normal
  C.shutdown TRANSACTIONAL
  D.startup force

8.会话意外终止时资源由谁释放?
  A.数据库管理员
  B.smon
  C.pmon
  D.ckpt

9.下列那些字典看到的信息更全?
  A.all_xxx
  B.dba_xxx
  C.v$xxx
  D.user_xxx

10.对控制文件描述正确的是?
  A.至少需要两个
  B.数据库运行是需要几个控制文件由参数决定
  C.控制文件不能热备
  D.控制文件的内容可以用文本编辑器修改

11.关于联机日志描述正确的是?
  A.组之间是并行读写
  B.至少需要一组
  C.记录恢复时所需要的信息
  D.如果有多组日志就不需做多元化了

12.如何将数据库置为归档模式?

  A.在数据库nomount状态下通过命令实现
  B.在数据库mount状态下通过命令实现
  C.在数据库open状态下通过命令实现
  D.通过设置参数实现

13. 关于归档日志描述正确的是?

  A.数据库恢复时需要
  B.所有归档日志都有用
  C.实例恢复时需要归档日志
  D.可以删除全备之后产生的归档日志

14. 数据库所能管理的数据量由什么决定?

  A.数据库版本
  B.数据库版本和创建数据库时决定
  C.数据文件的大小
  D.由控制文件决定

15.关于临时表空间描述正确的是?

  A.临时表空间中不能存在对象
  B.临时表空间是用来存放排序的中间结果
  C.临时表空间需要备份
  D.临时表空间可以提供读一致性

16.关于表空间描述正确的是?

  A.所有表空间都可以离线
  B.表空间的大小不能收缩
  C.只读数据库中本地管理的临时表空间可写
  D.创建表空间是可以指明打包因子

17.数据库逻辑结构由大到小排列正确的是?

  A.表空间,区,段,块
  B.表空间,段,区,块
  C.数据库,表空间,区,段,块
  D.数据库,表空间,段,区,块

18.关于高水位描述正确的是?

  A.不能回缩
  B.是数据的最高点
  C.是历史数据的最高点
  D.是数据增长的上限

19.关于表的描述正确的是?

  A.不能跨越表空间
  B.不能跨越数据文件
  C.常规表不能象分区表转化
  D.可以存储多列long数据类型

20.起用外键约束时描述正确的是?

  A.不能删除父表数据
  B.不能截断父表
  C.不能截断子表
  D.外键取值不可以重复


21、关于回退段的描述正确的为：
  A．回退段可以没有。
  B．回退段中可以建临时表。
  C．回退段大小是固定的。
  D．读一致性由回退段来实现的。

22、关于ROWID的描述正确的为：
    A．ROWID记录在数据的行头。
    B．ROWID并不存在，是计算的结果。
    C．ROWID对每一行来说是固定的，永不改变。
    D．ROWID是不可见的。

23、关于高水位的描述不正确的为：
   A．高水位不能回退，只能增长。
   B．高水位可以通过TRUNCATE来回退。
   C．高水位可以通过数据的重组来回退。
   D．高水位不能通过DELETE 来回退。

24. 对表的描述正确的为：
  A.表可以跨越数据文件。  B.表可以跨越数据库。
  C.表最少占一个数据块。  D.表最小为2K。

25.为什么会造成行迁移：
  A．插入太多的行。
  B．插入的行太长。
  C．修改使行变长了。

26、PCTFREE的描述正确的为：
  A．越大越好。   B．永不应为零。
  C．可以为零。   D．越小越好。

27、对REDO LOG的描述正确的为：
  A．越大越好。 B．越小越好。
  C．最少要一个组。D．最少要两个组。

28、快照太旧产生的直接原因：
  A．回退段太大了。         B.回退段太小了。
  C. 回退段的信息被覆盖了。 D.回退段挂起了。

29. 对索引的描述正确的为：
  A．越多性能越好。        B．越少性能越好。
  C．改动频繁的表要多加索引。    D．改动不频繁的表加索引会更好。

30. 关于权限下列哪项是正确的？
  A. SYS权限最高  B. SYSTEM权限最高
  C. 权限不能授予角色 D.角色是权限的集合

